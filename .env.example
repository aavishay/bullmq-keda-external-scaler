# Example environment configuration for KEDA Redis Bull Scaler
# Copy this file to .env and set the actual values

# Redis Configuration
REDIS_HOST=redis-service.bullmq-test.svc.cluster.local
REDIS_PORT=6379

# Bull Queue Names
WAIT_LIST=bull:test-queue:wait
ACTIVE_LIST=bull:test-queue:active

# Scaling Configuration
MAX_PODS=10

# For add-jobs.sh script
WAIT_QUEUE=bull:test-queue:wait
ACTIVE_QUEUE=bull:test-queue:active
